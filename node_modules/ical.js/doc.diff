diff --git a/lib/ical/component.js b/lib/ical/component.js
index 5bd0d29..070dac8 100644
--- a/lib/ical/component.js
+++ b/lib/ical/component.js
@@ -7,6 +7,9 @@
 
 (typeof(ICAL) === 'undefined')? ICAL = {} : '';
 (function() {
+  /**
+   * @class ICAL.icalcomponent
+   */
   ICAL.icalcomponent = function icalcomponent(data, parent) {
     this.wrappedJSObject = this;
     this.parent = parent;
@@ -69,6 +72,14 @@
       };
     },
 
+    /**
+     * Retrieves the first subcomponent of this component. If aType is passed,
+     * a specific type of subcomponent is retrieved, otherwise the first one
+     * available
+     *
+     * @method getFirstSubcomponent
+     * @param {String} aType    The type of subcomponent to retrieve
+     */
     getFirstSubcomponent: function getFirstSubcomponent(aType) {
       var comp = null;
       if (aType) {
diff --git a/lib/ical/ical.js b/lib/ical/ical.js
index b1ff551..9ff9743 100644
--- a/lib/ical/ical.js
+++ b/lib/ical/ical.js
@@ -12,8 +12,13 @@
   ICAL.newLineChar = "\n";
 
   /**
+   * @class ICAL
+   */
+
+  /**
    * Return a parsed ICAL object to the ICAL format.
    *
+   * @method stringify
    * @param {Object} object parsed ical string.
    * @return {String} ICAL string.
    */
@@ -24,6 +29,8 @@
   /**
    * Parse an ICAL object or string.
    *
+   * @method parse
+   * @param {Object} object parsed ical string.
    * @param {String|Object} ical ical string or pre-parsed object.
    * @param {Boolean} decorate when true decorates object data types.
    *
